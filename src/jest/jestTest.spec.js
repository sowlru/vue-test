// import { mount } from '@vue/test-utils'
// import jestTest from './jestTest.vue'

describe('Counter', () => {
  test('1 == 1', () => {
    expect(1).toBe(1)
  })
})

describe('First test', () => {
  test('1 == 1', () => {
    expect(1).toBe(1)
  })
})
